# Development-only Worker configuration. Copy to .dev.vars; never commit that file.
API_ORIGIN=http://localhost:3001
ORIGIN_AUTH_TOKEN=replace-with-a-long-random-local-secret
EMAILIT_API_KEY=mock-key
EMAILIT_SUBSCRIBER_LIST_ID=mock-list
